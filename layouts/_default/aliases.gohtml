<!DOCTYPE html>
<html lang="{{ .Site.Language.Lang }}">
<head>
    <meta charset="utf-8">
    <title>{{ .Permalink }}</title>
    <link rel="canonical" href="{{ .Permalink }}">
    
    {{- /* Only redirect if not on a preview domain */ -}}
    {{- if not (hasPrefix .Site.BaseURL "https://hugo-aerocool.netlify.app") -}}
    <meta name="robots" content="noindex">
    <meta http-equiv="refresh" content="0; url={{ .Permalink }}">
    {{- end -}}
</head>
<body>
    {{- if not (hasPrefix .Site.BaseURL "https://hugo-aerocool.netlify.app") -}}
    <p>Redirecting to <a href="{{ .Permalink }}">{{ .Permalink }}</a>...</p>
    {{- else -}}
    <p>Preview mode - no redirect. Target: <a href="{{ .Permalink }}">{{ .Permalink }}</a></p>
    {{- end -}}
</body>
</html>
