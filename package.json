{
  "name": "Aerocool",
  "version": "1.0.0",
  "devDependencies": {
    "@tailwindcss/cli": "4.3.0",
    "tailwindcss": "4.3.0"
  },
  "scripts": {
    "prebuild": "git submodule update --init --recursive",
    "build": "hugo --environment development --gc --minify",
    "build:production": "hugo --environment production --gc --minify",
    "predev": "git submodule update --init --recursive",
    "dev": "hugo server"
  },
  "dependencies": {
    "@netlify/database": "1.0.0"
  }
}
